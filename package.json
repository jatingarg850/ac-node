{
  "name": "server-node",
  "version": "1.0.0",
  "description": "Node.js server with PostgreSQL integration",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon src/index.js",
    "deploy": "bash deploy.sh",
    "build": "rm -rf dist && mkdir -p dist && cp -r src/* dist/ && cp package*.json dist/",
    "clean": "rm -rf dist",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
} 